<%= form_for @report do |f| %>
  <%= f.collection_select(:language_id, Language.all, :id, :name) %>
  <%= f.text_field :title, placeholder: "タイトル" %>
  <%= f.text_field :body, placeholder: "感想など" %>
  <%= f.submit %>
<% end %>